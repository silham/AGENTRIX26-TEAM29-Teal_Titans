"""Owner: M3. Corpus ingestion CLI: chunk → embed → write doc_chunks.

    python -m app.rag.ingest
"""

if __name__ == "__main__":
    # TODO(M3): load data/corpus/, chunk, embed via app.llm.embeddings, persist DocChunk rows.
    print("TODO(M3): implement corpus ingestion.")
