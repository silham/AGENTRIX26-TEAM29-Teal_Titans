"""Owner: M3. Query → relevant chunks with source URLs. TODO(M3)."""


def search(query: str, k: int = 4) -> list[dict]:
    # TODO(M3): vector search; return [{content, source_url, title, score}, ...].
    return []
