"""Owner: M4. Reminder node — appointment / expiry / inactivity nudges."""
from app.graph.state import GraphState


def reminder(state: GraphState) -> dict:
    # TODO(M4): derive reminders from steps/documents; return {"reminders": [...]}.
    return {}
