"""Owner: M3. RAG Knowledge node — requirements + citations."""
from app.graph.state import GraphState


def knowledge(state: GraphState) -> dict:
    # TODO(M3): rules.requirements(service) + retriever.search(goal)
    #           → return {"requirements": [...], "citations": [{title, source_url}]}.
    return {}
