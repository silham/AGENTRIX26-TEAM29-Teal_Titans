"""Owner: M5. Form Assistant node — explain / validate form fields."""
from app.graph.state import GraphState


def form(state: GraphState) -> dict:
    # TODO(M5): explain fields / validate sections via app.llm.groq_client.
    return {}
